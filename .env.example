# 이 파일을 .env 로 복사한 뒤 값을 채우세요. (.env 는 커밋 금지 — .gitignore에 포함됨)

# 서버
PORT=3000

# MariaDB 접속 정보 (DB 접속정보는 서버에만 둔다 — 확장에 넣지 않음)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=scare_alert
