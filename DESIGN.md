# 3,2,1 와! — 랜딩페이지 디자인 가이드

`jumpscare-landing.html` 수정 시 참고용 문서입니다. 새로운 섹션을 추가하거나 카피를 바꿀 때, 아래 원칙과 토큰을 벗어나지 않는 선에서 작업하면 톤이 유지됩니다.

---

## 0. 브랜드 이름 (표기 체계)

브랜드명을 용도에 따라 3가지 형태로 구분합니다. 풀 디스플레이형의 쉼표(,)·느낌표(!)·공백은 문장 속이나 도메인·패키지명 같은 기술 식별자에서 어색하거나 쓸 수 없기 때문입니다.

| 용도 | 표기 | 예 |
|---|---|---|
| **풀 디스플레이** (로고·메인·소개) | `3,2,1 와!` (와 앞 띄어쓰기 고정) | 히어로 헤드라인, 로고, 페이지 타이틀 |
| **짧은 브랜드명** (본문·문장 속) | `321와` | "321와는 무료입니다", "321와가 알려드려요" |
| **기술 슬러그·도메인** | `321wa` | `321wa.co.kr`, 패키지/URL/폴더명 |
| **저장소명** | `scare-alert`(확장) / `scare-alert-server`(백엔드) | 기존 내부 코드명 유지 |

**규칙**
- 로고·헤드라인·타이틀 등 임팩트 자리에는 `3,2,1 와!`(띄움 고정)를 씁니다.
- 문장 속에서 브랜드를 지칭할 땐 `321와`를 씁니다 (조사가 자연스럽게 붙어 따옴표가 불필요).
- 코드·도메인·파일명 등에는 `321wa`를 씁니다. (도메인 예정: `321wa.co.kr`)
- 로고에서 느낌표(!)를 "놀람" 포인트로 활용해도 좋습니다.
- **"점프스케어"는 브랜드명이 아니라 일반명사(놀라게 하는 그 순간)** 로만 씁니다. 제품을 가리킬 땐 `321와` / `3,2,1 와!`를 씁니다.

---

## 1. 컨셉 한 줄 요약

**"영화관 야간 상영 + 필름 릴 + 3초 카운트다운"**
공포영화를 좋아하지만 점프스케어가 무서운 사람을 위한 서비스라는 걸, 색과 모션 하나로도 알 수 있게 설계했습니다. 흔한 "다크모드 SaaS" 느낌이 아니라 "영화관 마퀴 조명 + 필름 스프로킷 홀"이라는 구체적인 세계관을 씁니다.

---

## 2. 컬러 토큰

| 변수 | 값 | 용도 |
|---|---|---|
| `--bg` | `#0a0a12` | 기본 배경 (인디고에 가까운 블랙, 순수 검정 아님) |
| `--bg-panel` | `#12121d` | 카드/프레임 배경 |
| `--bg-panel-2` | `#181826` | 스프로킷 홀 등 보조 배경 |
| `--ink` | `#f5f3ed` | 본문 텍스트 (따뜻한 오프화이트) |
| `--muted` | `#8b8996` | 보조 텍스트 |
| `--marquee` | `#f2b705` | **시그니처 액센트.** 영화관 마퀴 전구 색. CTA, 카운트다운 숫자, 강조 단어에만 사용 |
| `--alert` | `#ff4b3e` | 놀라는 순간의 플래시 전용. 평소 UI에는 쓰지 않음 (희소성 유지) |
| `--line` / `--line-strong` | 반투명 화이트 | 구분선, 테두리 |

**규칙**
- `--marquee`(앰버)는 "이 서비스가 알려주는 것"에만 씁니다: CTA 버튼, 카운트다운 숫자, 헤드라인 강조어.
- `--alert`(레드)는 오직 점프스케어 순간의 화면 플래시에만 사용합니다. 버튼이나 배지에 남용하면 앰버와의 대비, 즉 "평온 → 경고"의 의미가 사라집니다.
- 새 섹션을 추가할 때 새로운 액센트 컬러를 임의로 추가하지 마세요. 두 개의 액센트(앰버/레드)로 충분합니다.

---

## 3. 타이포그래피

- **디스플레이(제목)**: `Hahmlet` (weight 700) — 고대비 세리프. "고전 공포영화 타이틀" 무드의 시네마틱한 한글 서체. 헤드라인, 섹션 타이틀, 카운트다운 숫자에만 사용.
- **본문**: `IBM Plex Sans KR` — 헤드라인과 대비되는 차분한 그로테스크. 본문, 설명, 버튼 라벨.
- 두 서체 외에 새 폰트를 추가하지 않습니다. 디스플레이 서체는 "임팩트가 필요한 짧은 텍스트"에만 쓰고, 긴 문단에는 절대 사용하지 않습니다 (가독성 저하).

---

## 4. 레이아웃 구조

```
[필름 퍼포레이션 레일 — 좌우 고정, SVG 필름 스트립(베이스 띠 + 둥근 사각 구멍)]
nav (sticky, 블러 배경)
hero (유령 애니메이션 → 헤드라인 → CTA)
─── divider ───
problem block (텍스트 중심, 중앙 정렬)
─── divider ───
how it works (3-2-1 카운트다운 프레임 3개, 필름 프레임처럼 상하 점선 장식)
─── divider ───
platforms (플랫폼 이름 pill, 로고 없음 — 상표권 이슈 회피)
footer (설치 CTA + 후원 링크)
```

- 섹션 사이는 얇은 `divider` 한 줄로만 구분합니다. 배경색을 섹션마다 바꾸거나 큰 카드로 나누지 않습니다 — 미니멀함 유지가 이 디자인의 핵심입니다.
- 3단계 스텝은 숫자를 01/02/03이 아니라 실제 제품 기능인 **3→2→1 카운트다운**으로 표기합니다. 이건 장식이 아니라 실제 작동 방식을 반영한 구조입니다. 다른 숫자 나열이 필요하면 이 규칙(내용상 진짜 순서일 때만 번호 사용)을 그대로 적용하세요.

---

## 5. 히어로 애니메이션 시퀀스 (시그니처 요소)

이 페이지에서 유일하게 "화려한" 순간입니다. 다른 곳에는 이 정도 모션을 추가하지 마세요.

1. 페이지 로드 → 팝콘 먹는 평온한 유령 표시
2. 0.7초 후 → 우측 상단에 카운트다운 뱃지 등장, `3 → 2 → 1` 순서로 팝 애니메이션 (각 0.62초 간격)
3. 카운트 종료 → 유령 이미지 흔들림(shake) + 화면 전체 레드 플래시(0.35초) 동시 발생
4. 놀란 유령 이미지로 전환, 헤드라인 · 서브카피 · CTA 버튼이 순차적으로 페이드인
5. 이후 정적 상태로 고정. "다시 재생" 버튼으로만 재생 가능 (자동 반복 금지 — 산만함 방지)
6. `prefers-reduced-motion` 감지 시 애니메이션 생략하고 바로 최종 상태 표시

**첫 로드 지연 노출:** nav(헤더) · 좌우 필름 레일 · eyebrow("공포영화 시청 보조 확장 프로그램")는 처음엔 `opacity:0`으로 숨겨두고, 위 시퀀스가 끝나는 시점(`settle`)에 헤드라인·CTA와 함께 페이드인합니다. "다시 재생" 시 다시 숨겼다가 재노출하며, `prefers-reduced-motion`이면 즉시 최종 상태로 표시합니다. (구현: `.js-reveal` + `.is-hidden` 클래스 토글)

새 애니메이션을 추가할 때는 "왜 필요한가"를 먼저 확인하세요. 호버 마이크로 인터랙션 정도는 괜찮지만, 스크롤마다 등장하는 효과를 남발하면 이 페이지 특유의 절제된 톤이 깨집니다.

---

## 6. 카피 톤 가이드

- 문장은 짧고 능동태로. "~됩니다" 같은 수동적 표현보다 "알려드립니다", "지켜드릴게요"처럼 서비스가 하는 행동을 직접 말합니다.
- 기능을 팔지 말고 상황을 그립니다. ("AI 기반 정밀 감지 시스템" ❌ → "3초 전에 미리 알려드립니다" ✅)
- 유머는 담백하게. 유령·팝콘 소재를 활용하되 과하게 드립을 치지 않습니다 ("팝콘은 놓치지 마세요. 심장은 지켜드릴게요." 정도의 톤 유지).
- CTA 버튼 텍스트는 실제 동작을 그대로 씁니다. "시작하기" 대신 "크롬에 무료로 추가하기", "후원하기"처럼 사용자가 뭘 누르는지 명확하게.

---

## 7. 상표/법적 주의사항 (기존 프로젝트 방침 반영)

- Netflix, Wavve 등 플랫폼 로고 이미지는 사용하지 않습니다. 텍스트 pill로만 표기 (`--pill` 스타일 참고).
- 플랫폼 이름을 "제휴" 뉘앙스로 쓰지 않습니다. footer에 "특정 OTT 플랫폼과 제휴 관계가 없는 독립 프로젝트"라는 문구를 유지하세요.

---

## 8. 수정 시 체크리스트

- [ ] 새 액센트 컬러를 추가하지 않았는가? (앰버/레드 2개 유지)
- [ ] 새 폰트를 추가하지 않았는가? (Hahmlet / IBM Plex Sans KR 유지)
- [ ] 새 애니메이션이 정말 필요한 정보 전달 목적인가, 장식인가?
- [ ] 숫자 마커를 쓴다면 실제 순서/과정을 나타내는가?
- [ ] `prefers-reduced-motion` 대응이 유지되는가?
- [ ] 모바일(720px 이하)에서 필름 레일·그리드가 깨지지 않는가?

---

## 9. 구현 노트 (파일 구조)

- **스타일**: 모든 CSS는 `styles.css`에 있습니다(인라인 아님). 새 페이지는 `<link rel="stylesheet" href="styles.css">`로 토큰·컴포넌트를 공유합니다.
- **유령 이미지**: `nomal.svg`(평온·팝콘), `alert.svg`(놀람) — **투명 배경 + 흰색 fill** SVG. `<img>`로 삽입하며, `<img>`는 CSS로 색을 못 바꾸므로 색은 SVG 파일에 내장합니다. (이전 JPEG는 배경 박스 문제로 교체)
- **폰트**: Google Fonts CDN 로드(`Hahmlet`, `IBM Plex Sans KR`). 배포 전 개인정보(GDPR) 고려해 self-host 전환을 검토합니다.
- **필름 레일**: `.sprocket-rail`이 SVG data URI 타일(필름 베이스 `#181826` + 둥근 사각 구멍 `#0a0a12`)로 렌더링됩니다. 클래스명은 관례상 `sprocket-rail`을 유지합니다.
- **임시 프리뷰**: `font-preview.html`, `rail-preview.html`은 디자인 비교용 임시 파일 — 확정 후 삭제 대상입니다.
